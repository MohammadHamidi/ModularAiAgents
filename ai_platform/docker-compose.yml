# No local db service: use your existing PostgreSQL (set DATABASE_URL in .env or Coolify).
# Internal URL (same network): postgresql+asyncpg://USER:PASSWORD@HOST:5432/DBNAME
# Public URL (from outside):  postgresql+asyncpg://USER:PASSWORD@81.12.27.188:6363/DBNAME

services:
  chat-service:
    build:
      context: .
      dockerfile: ./services/chat-service/Dockerfile
      # ⚠️ SECURITY: Do NOT add args: section here for secrets!
      # Build args can leak into image history and build logs.
      # All secrets must be in environment: section below (runtime only).
      # If Coolify passes secrets as build args, move them to runtime env vars.
    # Optional: mount کنش CSV for query_konesh_csv (uncomment and set path to your CSV file)
    # volumes:
    #   - "./konesh_actions.csv:/app/konesh_actions.csv"
    environment:
      # =======================================================================
      # RUNTIME ENVIRONMENT VARIABLES (Secure - not in build history)
      # These are injected at container startup, not during build.
      # =======================================================================
      # Database: set DATABASE_URL to your existing PostgreSQL (required)
      DATABASE_URL: ${DATABASE_URL}
      
      # LiteLLM Configuration
      LITELLM_API_KEY: ${LITELLM_API_KEY:-sk-or}
      LITELLM_BASE_URL: ${LITELLM_BASE_URL:-https://openrouter.ai/api/v1}
      LITELLM_MODEL: ${LITELLM_MODEL:-google/gemini-2.5-flash-preview-09-2025}
      
      # Session Configuration
      MAX_SESSION_MESSAGES: ${MAX_SESSION_MESSAGES:-30}
      SESSION_TTL_SECONDS: ${SESSION_TTL_SECONDS:-14400}

      # Executor mode: pydantic_ai (default) or langchain_chain
      EXECUTOR_MODE: ${EXECUTOR_MODE:-pydantic_ai}
      
      # LightRAG Configuration
      LIGHTRAG_BASE_URL: ${LIGHTRAG_BASE_URL:-https://knowledgsafiran.pish.run/}
      LIGHTRAG_USERNAME: ${LIGHTRAG_USERNAME:-}
      LIGHTRAG_PASSWORD: ${LIGHTRAG_PASSWORD:-}
      LIGHTRAG_API_KEY_HEADER_VALUE: ${LIGHTRAG_API_KEY_HEADER_VALUE}
      LIGHTRAG_BEARER_TOKEN: ${LIGHTRAG_BEARER_TOKEN:-}
      # Path to کنش CSV (used if volume above is mounted as /app/konesh_actions.csv)
      KONESH_CSV_PATH: ${KONESH_CSV_PATH:-/app/konesh_actions.csv}
    ports:
      - "8001:8001"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8001/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  gateway:
    build:
      context: .
      dockerfile: ./services/gateway/Dockerfile
    environment:
      CHAT_SERVICE_URL: http://chat-service:8001
    depends_on:
      chat-service:
        condition: service_healthy
    ports:
      - "${GATEWAY_PORT:-8000}:8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
