# Pinned versions to speed up Docker pip install (avoids long resolver backtracking and timeouts).
# To update: run locally `pip install -r requirements.txt --upgrade` then `pip freeze` and adjust.

fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic-ai==0.0.14
griffe>=0.44.0  # required by pydantic-ai for docstring parsing
openai==1.55.3
httpx==0.28.1
python-dotenv==1.0.1
pydantic==2.12.5
pycryptodome==3.21.0  # For AES encryption/decryption
pyyaml==6.0.2  # For YAML configuration files

# LangChain for chain-based executor
langchain==0.3.8
langchain-openai==0.2.9
langchain-core==0.3.23
